<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.svetlanamarhefka">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <!-- Start Screen Activity. -->
        <activity
            android:name=".view.StartScreen"
            android:theme="@style/StartScreen">
            <intent-filter>
                <!-- This is the screen that launches. -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.GameInfo"
            android:theme="@style/InfoScreen">
        </activity>


        <!-- MMainGameModelGame Screen Activity -->
        <activity
            android:name=".view.MainGame"
            android:theme="@style/MainScreen">
        </activity>

    </application>
</manifest>